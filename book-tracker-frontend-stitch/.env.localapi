# Vite mode "localapi" (npm run dev -- --mode localapi): point the web app at a local backend.
# Gitignored by the root .gitignore `.env.*` rule. Values in .env still apply; this only overrides the API URL.
VITE_API_BASE_URL=http://127.0.0.1:8765
