# Doc Sync: Agent process install — 2026-09-12

Not a feature sprint. Records the install of the 7-agent process and the read-through audit that preceded it. No application code changed.

## Updates Made
- [x] `CLAUDE.md` → rewritten: ownership/privacy rule, agent table, node system, context-load table, verification table
- [x] `AGENTS_DESIGN.md`, `agents/*.md` (7), `specs/PM_PLAYBOOK.md` → created
- [x] `dependency-map.md` → created (curated contracts + generated appendix); `scripts/gen_dependency_map.py` → created
- [x] `features/_templates/*` (6) → created; `features/{auth,community,library,reading-stats}/index.md` → `git mv` from `context/*/README.md` with frontmatter
- [x] `repos/{api,web,mobile}/index.md` → created
- [x] `context/INDEX.md`, `context/LOAD_ME_FIRST.md` → paths updated, Sept 12 section + audit backlog + Known Issues
- [x] memory → `project_audit_2026_09.md`, `feedback_agent_models.md`, `feedback_book_tracker_only_context.md`; MEMORY.md index lines

## Divergences from the ERP process
| ERP | Here | Why |
|---|---|---|
| `specs/ plans/ qa/` output dirs | not created | ERP already marks them deprecated in favour of nodes |
| Industry-standards research rotation | dropped | ceremony a solo side project will not run |
| RLS / DPDP / Razorpay / MSG91 checks | dropped | not applicable |
| Manually maintained dependency map | generated appendix + curated top | the two `api.js` files make it mechanical |

## Open Issues Carried (first sprint backlog)
See `context/LOAD_ME_FIRST.md` → Recently Shipped (September 12, 2026). Top: unauthenticated `DELETE /books/{id}`, `push_router.py` `token_type` bug, 12 stale tests, venvs tracked in git.

## Needs PM Review
- Confirm Vercel's build command is `npm run build:ssg` (cannot see the dashboard).
- Decide whether `/auth/signup` + `/auth/login` (password auth, unused by both clients) should be removed or kept.
- Decide whether to run the DB repair for userbooks whose status was reset by the pre-May-4 rating bug.
